"""Repo-local issue ticket tracker.

Tickets are markdown files with YAML frontmatter stored in an issues/
directory inside the repository they belong to. The CLI discovers that
directory by walking up from the current working directory.
"""
